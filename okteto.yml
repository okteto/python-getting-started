icon: https://apps.okteto.com/movies/icon.png
build:
  web:
    context: .
deploy:
  - helm upgrade --install web chart --set web.image=${OKTETO_BUILD_WEB_IMAGE}

dev:
  web:
    command: bash
    forward:
      - 8080:8080
    sync:
      - .:/usr/src/app